This project parses the given html structure and creates the edgelist file for computing PageRank values in the next step at 'createEdgeList.java';

The 'TikaExtraction.java' and 'DealBig.java' files extracts all web page contents and remove the extra space lines
